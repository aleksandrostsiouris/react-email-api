import ping from "../routes/ping"
import preview from "../routes/preview"
import send from "../routes/send"
import keys from "../routes/keys"

export {
  ping as pingRoute,
  preview as previewRoute,
  send as sendRoute,
  keys as generateKeyRoute
}